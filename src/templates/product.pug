.header.content
  div
    text.input(lookup='name', placeholder='Name')

.form-content.form-1
  .form-title
    h2 Product Information
  .form-part
    .form-section
      .form-group.two-inputs
        text.input(lookup='slug', placeholder='Slug')
        text.input(lookup='sku', placeholder='SKU')
      .form-group
        textarea-control.input(lookup='description', placeholder='Description')
    .form-section
      h3 Images
      form-group.three-images
        // image-drop-control
        img(src='/img/hero-prod.png')
        img(src='/img/charging-case.png')
        img(src='/img/add-photo.png')

.form-content.form-2
  .form-title
    h2 Pricing and Availability
  .form-part
    .form-section
      h3 Price
      .form-group.two-inputs
        currency.input(lookup='price', placeholder='Price')
        currency.input(lookup='listPrice', placeholder='Compare Price')
      .form-group.two-inputs
        div.input
          span In Stock?
          checkbox-control(lookup='instock', placeholder='In Stock')
        text.input(lookup='quantity', placeholder='Quantity')
    .form-section
      h3 Shipping
      .form-group.three-inputs.dimensions
        text.input(lookup='dims.length', placeholder='Length')
        text.input(lookup='dims.width', placeholder='Width')
        text.input(lookup='dims.height', placeholder='Height')
      .form-group.quarter-quarter-half-inputs
        text.input(lookup='weight', placeholder='Weight')
        text.input(lookup='units', placeholder='Units')
        text.input(lookup='estimatedDelivery', placeholder='Estimated Delivery')

